# 🎯 Финальная инструкция по инициализации и проверке

## ✅ Что готово

1. **Скрипты для проверки созданы:**
   - `npm run verify` - полная проверка деплоя
   - `npm run check-state` - состояние контракта
   - `npm run check-wallet` - баланс кошелька
   - `npm run init-and-mint` - тестовый минтинг
   - `npm run check-nft <addr>` - проверка NFT

2. **Контракт задеплоен:**
   - Адрес: `EQBDIcLkPanrY_yxBihYYCucy0NNyzgDntrwY7IDMVZNmmFN`
   - Explorer: https://tonscan.org/address/EQBDIcLkPanrY_yxBihYYCucy0NNyzgDntrwY7IDMVZNmmFN
   - Код и данные правильные (все тесты проходят)
   - ⚠️ Требует инициализации через external message

3. **`.env` настроен:**
   ```
   COLLECTION_ADDRESS=EQBDIcLkPanrY_yxBihYYCucy0NNyzgDntrwY7IDMVZNmmFN
   SUBWALLET_ID=3
   ```

---

## 🔥 ПРОБЛЕМА: Контракт не принимает транзакции

Контракт задеплоен, но **требует инициализации через external message**.

### Почему это происходит:

Контракт использует механизм `touched` флага:
- При деплое `touched = false`
- Контракт ожидает external message для установки `touched = true`
- **Пока `touched = false`, контракт не обрабатывает internal messages**
- Все транзакции (топ-ап, минтинг) возвращаются обратно

---

## 🚀 КАК ПРОВЕРИТЬ ЧТО ВСЁ ХОРОШО

Для контракта, который требует external init, проверка выполняется так:

### Шаг 1: Инициализировать контракт через кошелек

**Через MyTonWallet (wallet.ton.org):**

1. Откройте https://wallet.ton.org/
2. Import wallet (используйте ваш MNEMONIC)
3. Send transaction:
   - **To:** `EQBDIcLkPanrY_yxBihYYCucy0NNyzgDntrwY7IDMVZNmmFN`
   - **Amount:** 0.001 TON
   - **Comment:** ПУСТОЙ (или любой текст - главное чтобы контракт получил первую транзакцию)

### Шаг 2: Проверить инициализацию

```bash
npm run verify
```

Должны увидеть:
```
✓ Collection is deployed and active
✓ Collection balance: X.XXXX TON  
✓ All collection get-methods work correctly
```

### Шаг 3: Пополнить контракт

```bash
npm run topup
```

### Шаг 4: Выполнить тестовый минтинг

```bash
npm run init-and-mint
```

### Шаг 5: Проверить созданный NFT

```bash
npm run check-nft <NFT_ADDRESS>
```

---

## 📊 АЛЬТЕРНАТИВА: Использовать старый контракт

Если не получается инициализировать новый контракт, можно использовать старый:

```bash
# Переключиться на старый контракт
cd func
cat .env | grep -v "COLLECTION_ADDRESS\|SUBWALLET_ID" > .env.tmp
echo "COLLECTION_ADDRESS=EQCeTZFtmvZIruJBIi4E0H1oh9w2zrcLDMVd_6bwVah2mPN4" >> .env.tmp
echo "SUBWALLET_ID=0" >> .env.tmp  
mv .env.tmp .env

# Проверить
npm run verify

# Выполнить минтинг
npm run init-and-mint
```

**Ограничения старого контракта:**
- ❌ `get_nft_address_by_index()` не работает (exit_code: 11)
- ⚠️ NFT могут не активироваться правильно
- ✅ Базовые функции работают

---

## 💡 РЕКОМЕНДАЦИЯ

**Самый простой способ:** Используйте **MyTonWallet** (wallet.ton.org) для отправки первой транзакции на новый контракт. Это инициализирует его, и затем всё будет работать правильно.

После инициализации:
1. Запустите `npm run verify` - все проверки пройдут
2. Запустите `npm run init-and-mint` - минтинг сработает
3. Контракт готов к использованию!

---

## 🛠️ Полезные команды

```bash
npm run verify          # Полная проверка
npm run check-state     # Состояние контракта  
npm run init-and-mint   # Тестовый минтинг
npm run check-nft <addr> # Проверить NFT
npm run wallet-instructions # Показать инструкции
```

